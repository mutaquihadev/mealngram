<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/title_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="20sp"
        android:background="@color/color_default"
        android:textColor="@color/inverter_color"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="@id/title_section"
        app:layout_constraintBottom_toBottomOf="@id/title_section"
        app:layout_constraintTop_toTopOf="@id/title_section">

    <ImageButton
        android:id="@+id/button_section_close"
        android:layout_width="match_parent"
        android:padding="8dp"
        android:src="@drawable/ic_baseline_keyboard_arrow_up_24"
        android:background="@android:color/transparent"
        android:tint="@color/inverter_color"
        android:layout_height="match_parent"
         />

        <ImageButton
            android:id="@+id/button_section_open"
            android:layout_width="match_parent"
            android:visibility="gone"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:tint="@color/inverter_color"
            android:src="@drawable/ic_baseline_keyboard_arrow_down_24"
            android:background="@android:color/transparent"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>